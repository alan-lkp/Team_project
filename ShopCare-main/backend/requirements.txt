# ============================================================
# ShopCare 后端依赖 (backend/requirements.txt)
#
# 装法: pip install -r backend/requirements.txt
#
# 说明:
#   * 上面 6 个是**服务本身**的依赖, 装完就能把 API 跑起来(模型不可用时走降级).
#   * 下面的"模型侧"依赖按你实际要用哪个模型来装 —— 只用 RF 就不必装 torch
#     (torch 装起来几百 MB, 而且 CPU 版和 CUDA 版要挑, 没必要就不用装).
#   * 训练用的依赖(02/03/04 三个目录)和这里的服务依赖是**分开考虑**的:
#     训练可以在装了 GPU 的机器上跑, 服务部署到普通机器上只带推理需要的东西.
# ============================================================

# ---------------- 服务必需 ----------------
fastapi>=0.110.0
uvicorn>=0.27.0
pydantic>=2.5.0
PyMySQL>=1.1.0
redis>=5.0.0
PyJWT>=2.8.0

# ---------------- 按需: 各模型阶段 ----------------
# 02-rf (随机森林 + TF-IDF)
numpy>=1.24.0
scikit-learn>=1.3.0
joblib>=1.3.0

# 03-fasttext
fasttext-wheel>=0.9.2
jieba>=0.42.1

# 04-bert (只有要用 BERT 时才需要)
# torch>=2.0.0
# transformers>=4.30.0

# ---------------- 可选 ----------------
# 数据分析(01-data/data_eda.py)
# pandas>=2.0.0